name: recipes_repository
description: A repository to manage cocktails and meals API calls
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: 3.3.7

dependencies:
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  meals_and_drinks_api_client:
    path: ../meals_and_drinks_api_client

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^3.1.0
